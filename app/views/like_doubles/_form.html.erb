<%= form_with(model: like_double, local: true) do |form| %>
  <% if like_double.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(like_double.errors.count, "error") %> prohibited this like_double from being saved:</h2>

      <ul>
      <% like_double.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
